{
    "version": "0.2.0",
    "console": "internalConsole",
    "configurations": [
        {
            "name": "LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/z80_assembler",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "make",
            "args": [
            ]
        }
    ]
}
